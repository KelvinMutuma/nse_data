<%= form_for(@market) do |f| %>
  <% if @market.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@market.errors.count, "error") %> prohibited this market from being saved:</h2>

      <ul>
      <% @market.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :equity_turnover %><br>
    <%= f.text_field :equity_turnover %>
  </div>
  <div class="field">
    <%= f.label :market_capitalization %><br>
    <%= f.text_field :market_capitalization %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
