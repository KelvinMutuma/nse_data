<%= form_for(@mover) do |f| %>
  <% if @mover.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@mover.errors.count, "error") %> prohibited this mover from being saved:</h2>

      <ul>
      <% @mover.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :mover %><br>
    <%= f.text_field :mover %>
  </div>
  <div class="field">
    <%= f.label :volume %><br>
    <%= f.number_field :volume %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
